/* src\Components\Notification.svelte generated by Svelte v3.46.4 */
import {
	SvelteComponent,
	create_component,
	destroy_component,
	init,
	mount_component,
	safe_not_equal,
	transition_in,
	transition_out
} from "../../_snowpack/pkg/svelte/internal.js";

import { ToastNotification } from "../../_snowpack/pkg/carbon-components-svelte.js";

function create_fragment(ctx) {
	let toastnotification;
	let current;

	toastnotification = new ToastNotification({
			props: {
				style: "z-index: 100000000; position: relative;",
				title: /*notification*/ ctx[0].title,
				subtitle: /*notification*/ ctx[0].subtitle,
				kind: /*notification*/ ctx[0].type
			}
		});

	toastnotification.$on("close", /*handleButtonClick*/ ctx[1]);

	return {
		c() {
			create_component(toastnotification.$$.fragment);
		},
		m(target, anchor) {
			mount_component(toastnotification, target, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			const toastnotification_changes = {};
			if (dirty & /*notification*/ 1) toastnotification_changes.title = /*notification*/ ctx[0].title;
			if (dirty & /*notification*/ 1) toastnotification_changes.subtitle = /*notification*/ ctx[0].subtitle;
			if (dirty & /*notification*/ 1) toastnotification_changes.kind = /*notification*/ ctx[0].type;
			toastnotification.$set(toastnotification_changes);
		},
		i(local) {
			if (current) return;
			transition_in(toastnotification.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(toastnotification.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(toastnotification, detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { notification = {} } = $$props;
	let { onRemove = null } = $$props;

	const handleButtonClick = () => {
		onRemove();
	};

	$$self.$$set = $$props => {
		if ('notification' in $$props) $$invalidate(0, notification = $$props.notification);
		if ('onRemove' in $$props) $$invalidate(2, onRemove = $$props.onRemove);
	};

	return [notification, handleButtonClick, onRemove];
}

class Notification extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { notification: 0, onRemove: 2 });
	}
}

export default Notification;